/** Shared Recharts styling constants -- originally on the Reports page, now reused by InteractiveChart too so every chart in the app looks consistent. */
export const AXIS_TICK = { fill: "#94a3b8", fontSize: 11.5 };
export const CHART_MARGIN = { top: 12, right: 16, left: 4, bottom: 8 };
